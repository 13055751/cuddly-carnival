import { Context, Schema } from 'koishi';
import { Message, Renderer, RenderMessage, RenderOptions } from 'koishi-plugin-chatluna';
import { Config } from '..';
export declare class ImageRenderer extends Renderer {
    protected readonly ctx: Context;
    protected readonly config: Config;
    private __page;
    private _marked;
    constructor(ctx: Context, config: Config);
    private _page;
    render(message: Message, options: RenderOptions): Promise<RenderMessage>;
    private _renderMarkdownToHtml;
    private _textToQRCode;
    schema: Schema<"image", "image">;
}
export declare function renderTemplate(template: string, data: Record<string, string>): string;
export declare function randomArrayItem<T>(array: T[]): T;
export declare function escapeBrackets(text: string): string;
