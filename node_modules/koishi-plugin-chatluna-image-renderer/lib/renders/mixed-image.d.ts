import { Context, Schema } from 'koishi';
import { Message, Renderer, RenderMessage, RenderOptions } from 'koishi-plugin-chatluna';
import { Config } from '..';
export declare class MixedImageRenderer extends Renderer {
    protected readonly ctx: Context;
    protected readonly config: Config;
    private __page;
    private _marked;
    constructor(ctx: Context, config: Config);
    private _page;
    render(message: Message, options: RenderOptions): Promise<RenderMessage>;
    private _matchText;
    private _renderMarkdownToImage;
    private _renderMarkdownToHtml;
    private _textToQrcode;
    schema: Schema<"mixed-image", "mixed-image">;
}
