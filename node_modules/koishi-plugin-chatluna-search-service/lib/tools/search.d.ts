import { Tool } from '@langchain/core/tools';
import { SearchManager } from '../provide';
import { PuppeteerBrowserTool } from './puppeteerBrowserTool';
import { Embeddings } from '@langchain/core/embeddings';
import { SummaryType } from '../types';
import { ChatLunaChatModel } from 'koishi-plugin-chatluna/llm-core/platform/model';
export declare class SearchTool extends Tool {
    private searchManager;
    private browserTool;
    private embeddings;
    private llm;
    private summaryType;
    name: string;
    description: string;
    private _textSplitter;
    constructor(searchManager: SearchManager, browserTool: PuppeteerBrowserTool, embeddings: Embeddings, llm: ChatLunaChatModel, summaryType: SummaryType);
    _call(arg: string): Promise<string>;
    private fetchSearchResult;
    private _reRankDocuments;
}
export declare function generateFakeSearchResult(query: string, llm: ChatLunaChatModel): Promise<import("@langchain/core/messages").AIMessageChunk>;
