/**
 * 预处理内容，移除可能的 markdown 代码块标记
 */
export declare function preprocessContent(content: string): string;
/**
 * 尝试解析 JSON，失败时返回 null
 */
export declare function tryParseJSON(content: string): any;
/**
 * 尝试修复常见的 JSON 格式错误
 */
export declare function attemptToFixJSON(content: string): string;
export declare function removeProperty<T extends object, K extends keyof T>(value: T, properties: K[]): Omit<T, K>;
