export declare class BufferText {
    private readonly sleepTime;
    private readonly prefix?;
    private readonly postfix?;
    private queue;
    private currentText;
    private lock;
    private isEnd;
    private isTextStarted;
    private rawText;
    constructor(sleepTime?: number, prefix?: string, postfix?: string);
    addText(text: string): Promise<void>;
    private getText;
    private processChar;
    get(): AsyncGenerator<string, void, unknown>;
    splitByMarkdown(): AsyncGenerator<string, void, unknown>;
    splitByPunctuations(): AsyncGenerator<string, void, unknown>;
    getCached(endText?: string): AsyncGenerator<string, void, unknown>;
    end(): void;
}
