export declare const name = "@initencounter/vits";
import { Context, Service, h } from 'koishi';
declare module 'koishi' {
    interface Context {
        vits: Vits;
    }
}
declare abstract class Vits extends Service {
    constructor(ctx: Context);
    abstract say(options: Vits.Result): Promise<h>;
}
declare namespace Vits {
    interface Config {
    }
    interface Result {
        input: string;
        speaker_id?: number;
        output?: h;
    }
}
export default Vits;
